"""The matching engine: pack, rules, text, matcher, stand-off annotations.

Importing this package pulls in nothing beyond `rapidfuzz` and `pyyaml`; the
ingest extra is deliberately not reachable from here.
"""
